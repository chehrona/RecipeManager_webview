import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    addButton: {
        position: 'absolute',
        bottom: 10,
        width: 40,
        justifyContent: 'center',
        alignItems: 'center',
    },
    addIcon: {
        fontSize: 20,
        textAlign: 'center',
    },
});
