import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    backButton: {
        position: 'absolute',
        bottom: 10,
        width: 40,
        justifyContent: 'center',
        alignItems: 'center',
    },
    backIcon: {
        fontSize: 20,
        textAlign: 'center',
    },
});
