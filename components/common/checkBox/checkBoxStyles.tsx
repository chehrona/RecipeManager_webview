import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    checkBox: {
        borderRadius: 30,
        borderWidth: 1,
        borderColor: 'grey',
        padding: 1,
    },
    text: {
        fontSize: 16,
        color: 'white',
    },
});
